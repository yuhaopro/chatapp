import 'package:flutter/material.dart';
class ScreenFunctions {
  static login(GlobalKey<FormState> formkey, String? email, String? password) {
    if (formkey.currentState!.validate())
      {

      }
  }

  static register(GlobalKey<FormState> formkey, String? username, String? email, String? password, String? confirmpassword) {
    if (formkey.currentState!.validate())
      {

      }
  }

}